---
title: lintSemverRanges
---

When using `synopkg lint`, enables checking that all the semver ranges in your `package.json` files are valid.

## Default value

```json title=".synopkgrc"
{
  "lintSemverRanges": true
}
```

:::note

There is no equivalent CLI Option for this configuration.

:::
