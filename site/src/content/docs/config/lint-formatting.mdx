---
title: lintFormatting
---

When using `synopkg lint`, enables checking the formatting of `package.json` files matches the output of `synopkg format`.

## Default value

```json title=".synopkgrc"
{
  "lintFormatting": true
}
```

:::note

There is no equivalent CLI Option for this configuration.

:::
