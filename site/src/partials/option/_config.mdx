import { Code } from 'astro:components';

Synopkg uses [cosmiconfig](https://github.com/cosmiconfig/cosmiconfig) to discover your config file, but a custom location can be provided instead.

<Code code={`synopkg ${props.command} --config ./config/.synopkgrc`} lang="bash" />
