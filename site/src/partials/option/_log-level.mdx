import { Code } from 'astro:components';

Verbose logging can be enabled by prefixing synopkg with the following environment variable:

<Code code={`SYNOPKG_VERBOSE=true synopkg ${props.command}`} lang="bash" />
